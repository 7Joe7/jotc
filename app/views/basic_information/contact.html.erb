<% provide(:title, 'Contact') %>
<div class="row">
    <div class="span4">ahoj</div>
    <div class="span12">
        <% if logged_in? %>
            <section class="user_info">
                <%= render 'shared/user_info' %>
            </section>
        <% end %>
        <section class="message_form">
            <%= render 'shared/message_form' %>
        </section>
    </div>
</div>

